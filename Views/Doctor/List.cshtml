@model IEnumerable<User>

@{
    ViewData["Title"] = "Doctors";
}




    <div class="panel panel-default">
        <div class="panel-heading">
            List of doctors
        </div>
        <div class="panel-body">
            <table class="table table-bordered">
                <thead>
                    <tr>
                        <th>UserName</th>
                        <th>Email</th>
                        <th>Clinic</th>
                        <th>Is email confirmed?</th>
                        <th></th>

                    </tr>
                </thead>
                <tbody>
                    @foreach (var doctor in Model)
                    {
                        <tr>
                            <td>
                                @doctor.UserName
                            </td>
                            <td>
                                @doctor.Email
                            </td>
                            <td>
                                @if (doctor.Clinic != null)
                                {
                                    @doctor.Clinic.Name
                                }
                            </td>
                            <td>
                                @(doctor.EmailConfirmed ? "Yes" : "No")
                            </td>
                            <td>
                                <a href="#" class="btn-outline-primary">Edit</a>
                                <a href="#" class="btn-outline-danger">Delete</a>
                            </td>
                        </tr>
                    }

                </tbody>
            </table>
            <a href="#" class="btn-outline-info">Create</a>
        </div>
    </div>
@*<ng-view></ng-view>*@

<script>
    var MvcModel = @Html.Raw(Newtonsoft.Json.JsonConvert.SerializeObject(Model, Newtonsoft.Json.Formatting.Indented,
new Newtonsoft.Json.JsonSerializerSettings
{
    ReferenceLoopHandling = Newtonsoft.Json.ReferenceLoopHandling.Ignore
}) as String);
</script>